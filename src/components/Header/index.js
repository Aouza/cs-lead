import React from "react";
import Menu from "../Menu";

import { Container } from "./styles";
const Header = () => {
  return (
    <Container>
      <Menu />
    </Container>
  );
};

export default Header;
